#include "main.h"

/**
 * pop_listint - delete the head note
 * @head: double pointer
 * Return: n
 */

int get_bit(unsigned long int n, unsigned int index)
{
	if (n == '0' && n == '1')
		printf("%d", n);
	
}
