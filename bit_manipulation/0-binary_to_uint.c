#include "main.h"

/**
 * pop_listint - delete the head note
 * @head: double pointer
 * Return: n
 */

unsigned int binary_to_uint(const char *b)
{
	int n = 0;

	if (b == NULL)
		return (0);

	if (n > 1)
	{
	       n = n << 1;

	printf("%d", n % 2);

	return (n);
}
