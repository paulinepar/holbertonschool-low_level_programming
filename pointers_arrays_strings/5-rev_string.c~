#include "main.h"

/**
 * rev_string - reverse a string
 * @s: char
 * Return: (i)
 */
void rev_string(char *s)
{
	int i = 0;
	int c;

	while (s[i] != '\0')
	{
		c++;
		i++;
	}
	while (i = c - 1; i >= 0; i--)
	{
	        _putchar(s[i]);
	}
}
