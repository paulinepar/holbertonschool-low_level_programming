#include "main.h"

/**
 * reverse_array - reverse the content
 * @a: char
 * @n: char
 * Return: (i)
 */

char *string_toupper(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	if (s[i] >= 'a' && s [i] <= 'z')
	{
		s[i] = s[i] -32;
	}
	return (0);
}
