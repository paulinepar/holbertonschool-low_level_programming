#include "main.h"

/**
 * _strcmp - concatene two string
 * @s2: char
 * @s1: char
 * Return: (i)
 */

int _strcmp(char *s1, char *s2)
{
	int i = 0;
	int k = 0;

	while (s1[k] != '\0' && s2[i] != '\0')
	{
	if (s1[k] != s2[i])
	{
		printf("%d\n", 
	{

	return (0);
}
