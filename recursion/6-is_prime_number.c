#include "main.h"
#include <stdio.h>

/**
 * _moon - print recurse
 * @c: string
 * @n: string
 * Return: 0
 */
int _moon(int c, int n)
{
	if (c < n)
	{
		if ( n % c == 0)
		{
			return (0);
		}
		else
			
int is_prime_number(int n)
