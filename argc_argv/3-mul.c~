#include <stdio.h>
#include <stdlib.h>

/**
 * main - argc argv
 * @argc: char
 * @argv: char
 * Return: (i)
 */

int main(int argc, char *argv[])
{
	(void) argv;
	int n = atoi(argv[]);
	int res;

	if (i > 1)
	{
		printf("%d\n", (n * n) = res);
	}
	return (1);
}
