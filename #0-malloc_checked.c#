#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * maloc_checked - allocate memory
 * @b: int
 * Return: Nothing.
 */

void *malloc_checked(unsigned int b)
{
	unsigned int *p;

	p = malloc(b);
		if (p == NULL)
			return (98);
	return (p);
}
